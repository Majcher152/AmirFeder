@model ProductsListViewModel

@foreach (var i in Model.Products){
    @*<div class="card card-outline-primary m-1 p-1">
        <div class="bg-faded p-1">
            @i.Name
        </div>
        <div class="card-text p-1">
            @i.Description
        </div>
    </div>*@
    @Html.Partial("ProductSummary", i);
}<div page-action="List" page-classes-enabled="true" page-class="btn" page-class-normal="btn-light"
      page-class-selected="btn-primary" page-url-category="@Model.CurrentCategory" class="btn-group pull-right m-1">
    </div>
